{
  "name": "defi-ai-agent",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --target=bun",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "hono": "^4.1.0",
    "openai": "^4.28.0",
    "zod": "^3.22.4",
    "viem": "^2.8.0",
    "pino": "^8.19.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@biomejs/biome": "^1.6.0",
    "vitest": "^1.3.1"
  }
}
